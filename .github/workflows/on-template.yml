name: On Template

on:
  push:
    branches:
      - main

jobs:
  go-publish:
    name: On use template

    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Replace namespaces if using as template
        run: ./plubo init_template

      - name: Commit
        uses: actions-x/commit@v6
        with:
          name: 'Plugin Init - Replace namespaces'
          force: true
          branch: 'main'
